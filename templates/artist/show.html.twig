{% extends 'base.html.twig' %}

{% block title %}Artist{% endblock %}

{% block body %}
    <h1>Artist</h1>

    <table class="table" id="artistShow" data-id="{{ artist.id }}">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artist.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ artist.nom }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ artist.pays }}</td>
            </tr>
            <tr>
                <th>Style</th>
                <td>{{ artist.style }}</td>
            </tr>
            <tr>
                <th>Presentation</th>
                <td>{{ artist.presentation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('artist_index') }}">back to list</a>
{% if is_granted(["ROLE_ADMIN"]) %}
    <a href="{{ path('artist_edit', {'id': artist.id}) }}">edit</a>
 
    {{ include('artist/_delete_form.html.twig') }}

    {% endif %}

    <p><button id="styleButton" type="button" class="btn btn-outline-warning">D'autres artistes du même style</button></p>
    <div id="sameStyleArtist"></div>
{% endblock %}

{% block javascripts %}
         {{ encore_entry_script_tags('sameStyle') }}

        {% endblock %}
